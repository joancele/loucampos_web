---
import { ClientRouter } from "astro:transitions";
import "../styles/global.css";

interface Props {
  title?: string;
}

const { title = "Lou Campos - Artist Portfolio" } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
      rel="stylesheet"
    />
    <ClientRouter />
  </head>
  <body
    class="bg-background-light dark:bg-background-dark text-text-light dark:text-text-dark min-h-[100dvh] flex flex-col transition-colors duration-300"
  >
    <div
      id="top"
      class="mx-auto max-w-[2048px] w-full min-h-[100dvh] flex flex-col bg-background-light dark:bg-background-dark shadow-[0_0_40px_rgba(0,0,0,0.1)] dark:shadow-[0_0_40px_rgba(0,0,0,0.5)] relative overflow-x-hidden"
    >
      <header
        class="w-full relative px-6 py-6 md:px-12 md:py-10 bg-background-light dark:bg-background-dark z-20"
      >
        <div
          class="flex flex-col landscape:flex-row items-center justify-center relative lg:hidden w-full"
        >
          <button
            class="absolute right-0 top-1/2 -translate-y-1/2 p-2 text-dark-grey dark:text-text-dark focus:outline-none landscape:z-10 flex items-center justify-center"
            id="mobile-menu-btn"
          >
            <span class="material-symbols-outlined text-3xl">menu</span>
          </button>
          <div
            class="flex flex-col landscape:flex-row items-center gap-1 landscape:gap-6"
          >
            <a class="flex items-center -mb-1 landscape:mb-0" href="/">
              <img
                src="/logo.svg"
                alt="ロウ カンポス"
                class="h-[50px] w-auto"
              />
            </a>
            <div
              class="h-[1px] w-24 landscape:h-8 landscape:w-[1px] bg-accent-pink my-1 landscape:my-0"
            >
            </div>
            <a
              class="font-sans font-normal text-2xl text-dark-grey dark:text-text-dark tracking-[0.2em] uppercase whitespace-nowrap"
              href="/"
            >
              Lou Campos
            </a>
          </div>
        </div>
        <div class="hidden lg:flex items-center justify-between gap-8">
          <div class="flex items-center gap-6">
            <a class="flex items-center" href="/">
              <img
                src="/logo.svg"
                alt="ロウ カンポス"
                class="h-[60px] w-auto"
              />
            </a>
            <div class="h-8 w-[1px] bg-accent-pink"></div>
            <a
              class="font-sans font-light text-3xl text-dark-grey dark:text-text-dark tracking-[0.2em] uppercase whitespace-nowrap"
              href="/"
            >
              Lou Campos
            </a>
          </div>
          <nav>
            <ul class="flex flex-wrap justify-end gap-10">
              <li>
                <a
                  class:list={[
                    "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                    currentPath.startsWith("/works")
                      ? "font-bold text-accent-pink"
                      : "font-medium text-dark-grey dark:text-text-dark",
                  ]}
                  href="/works"
                  data-i18n="nav.works"
                >
                  Works
                </a>
              </li>
              <li>
                <a
                  class:list={[
                    "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                    currentPath.startsWith("/about")
                      ? "font-bold text-accent-pink"
                      : "font-medium text-dark-grey dark:text-text-dark",
                  ]}
                  href="/about"
                  data-i18n="nav.about"
                >
                  About
                </a>
              </li>

              <li>
                <a
                  class:list={[
                    "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                    currentPath.startsWith("/contact")
                      ? "font-bold text-accent-pink"
                      : "font-medium text-dark-grey dark:text-text-dark",
                  ]}
                  href="/contact"
                  data-i18n="nav.contact"
                >
                  Contact
                </a>
              </li>
            </ul>
          </nav>
        </div>
        <nav
          class="hidden lg:hidden w-full mt-8 border-t border-gray-100 dark:border-gray-800 pt-6 animate-fade-in"
          id="mobile-menu"
        >
          <ul class="flex flex-col items-center gap-6">
            <li>
              <a
                class:list={[
                  "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                  currentPath.startsWith("/works")
                    ? "font-bold text-accent-pink"
                    : "font-medium text-dark-grey dark:text-text-dark",
                ]}
                href="/works"
                data-i18n="nav.works"
              >
                Works
              </a>
            </li>
            <li>
              <a
                class:list={[
                  "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                  currentPath.startsWith("/about")
                    ? "font-bold text-accent-pink"
                    : "font-medium text-dark-grey dark:text-text-dark",
                ]}
                href="/about"
                data-i18n="nav.about"
              >
                About
              </a>
            </li>

            <li>
              <a
                class:list={[
                  "font-sans text-sm hover:text-accent-pink transition-colors tracking-[0.15em] uppercase",
                  currentPath.startsWith("/contact")
                    ? "font-bold text-accent-pink"
                    : "font-medium text-dark-grey dark:text-text-dark",
                ]}
                href="/contact"
                data-i18n="nav.contact"
              >
                Contact
              </a>
            </li>
          </ul>
        </nav>
      </header>

      <slot />

      <div
        class:list={[
          "fixed transition-all duration-500 z-50",
          currentPath === "/contact" || currentPath === "/contact/"
            ? "bottom-10 left-1/2 -translate-x-1/2 scale-110"
            : "bottom-6 right-6",
        ]}
      >
        <button
          class="p-4 rounded-full bg-white dark:bg-gray-800 text-dark-grey dark:text-white shadow-xl hover:shadow-2xl transition-all duration-300 focus:outline-none flex items-center justify-center border border-gray-100 dark:border-gray-700 group"
          id="theme-toggle"
          aria-label="Toggle dark mode"
        >
          <span
            class="material-symbols-outlined text-2xl block dark:hidden group-hover:rotate-12 transition-transform"
          >
            dark_mode
          </span>
          <span
            class="material-symbols-outlined text-2xl hidden dark:block group-hover:-rotate-12 transition-transform"
          >
            light_mode
          </span>
        </button>
      </div>
      <script src="../scripts/layouts/Layout.js"></script>
    </div>
  </body>
</html>
